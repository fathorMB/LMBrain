# Reviews

Reviews are created by the Project Lead after the user asks for review of completed offloaded work. They compare source changes and evidence against a `SPEC-*` document.

Only an `accepted` review permits its linked spec to become `accepted`.

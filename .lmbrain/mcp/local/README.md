# Local MCP Implementations

If a project-specific MCP is implemented in this repository, keep its source here or link to its actual location from its `MCP-*.md` specification. The specification remains the source of truth for its intended capability and permissions.
